# Xander's GPTi84 Plus

:84p: 

> "The things I'd make if I were a student without morals..."

---

Hey all,

Uni student currently wrapping up this degree, and I had this idea the other night when sitting there, looking at the calculator gathering dust on my shelf (*the same one I haven't picked up in the last year or two since I've been done with the physics classes and whatnot*): "I can totally still use this for something..."

Well, a few days later I'm out the other side with a new demo. The core idea was simple:
**if this 2.5mm I/O port is just a TRS jack at the end of the day, running voltages along tip/ring through the wire, theoretically there's a way to bit-bang my way "into the Matrix."**

(*...and **YES**, the coolest candidate for this felt like LLM inference. I always dreamed of writing a program that could "answer arbitrary problems" and I used to get DAMN clever with my variables in relativity and quantum classes haha.)*

I quickly learned that there is, in fact, a bunch of people doing exactly that right now! And it's right here on Cemetech.
So... hello! Nice to meet you guys. I'm new here but man is this stuff interesting.

---

Anyways, back to the project. I like calling it my "GPTi84 Plus" because it just feels like the name is right there haha.

## The first hardware experiments:

I got started like you'd expect--disassembling my calculator immediately so I could see what I was working with on the inside.
How much space I'd have for third-party components, if there was extra room outside of the main PCB to stash cables or PCBs, etc.

Didn't take long to figure out how to pry the sucker open and look around. And, admittedly, I... probably got a bit too excited at this point!
Started instantly messing about with wiring a microcontroller up to the mainboard and trying to get something to fit nicely in the case shell.

![Xander's first calculator progress photo](../xander_lab_photos/IMG_3512.jpeg)

You can see my first attempt above. At this point I was running an ESP32-S3 w/ soldered headers (which I promptly tried prying off and I'll get to that in a moment) and life was looking up.

The real problems started when I actually tried doing anything with this little MC lol.

So my first thought was a bit of a distraction. I figured I needed a way to consistently power the calculator while I had the internals ripped open and was iterating on the board.

I thought "simple enough right? Just solder headers to those bottom pads and profit." Some of you probably saw the problem there right away, so I'll save you the pain--YES, I eventually did realize nothing was going to work until I shorted the spring pins visible in the middle of that last image there. Which eluded me a bit at the start, I must admit! Eventually I figured that out though, and I was left with a working (albeit still-stock) calculator running on a bench power supply (*ignore the crazy battery pack side-experiment in the first image haha*):

![Exposed Ti84 Plus calculator showing pin shorts](../xander_lab_photos/IMG_3518.jpeg)

![Sealed Ti84 Plus calculator connected to bench power supply](../xander_lab_photos/IMG_3519.jpeg)

Once I had the thing reliably powered without the endless churn of battery cycling... I immediately broke my ESP32-S3 by attempting to desolder the machine-installed headers from the board and breaking a pin used to manage the boot cycle. 🥴

So... fast-forward a day or two and I ended up with a development harness that looked like this (*yes, very hidden, very flush I know...*):

![Pico 1 W-powered Ti84 Plus breadboard harness](../xander_lab_photos/IMG_3526.jpeg)

The real win with this new development harness was that little orange 3.5mm female jack I managed to source from an old Bluetooth audio transceiver I found laying around in the junk drawer. Because I, by sheer luck, had a Sennheiser headphone cable with 2.5mm TRRS that terminated on the other end with a 3.5mm male adapter! You can see that plugged in above here.

Honestly, the breadboard was pretty unreliable (pins would disconnected at random, shoddy contact across the board) but it was what let me start actually experimenting with microcontroller firmware so it was a crucial part of the project at this stage.

Speaking of. Time to start talking about how I eventually ended up cracking the code of this ancient beast on the software side of things.

## The Code:

Originally I started writing my firmware in C because that's just the embedded muscle memory for hardware projects. And with the ESP32 iteration, I figured it was going to be trivial via ESP-IDF. Then, yknow, I broke the damn thing. So I had to re-evaluate for the pivot to Pico. 

Now, I could have chosen C again here, but honestly? I had never used MicroPython and kinda wanted an excuse to see what "embedded JIT" looks like hands-on (*hint: highly constrained but usable and fun!*)

So that's what I settled on, and I got to work figuring out what the hell I was actually going to do here to make any of this vision pan out the way I imagined.

I started simple. Needed access to a simple set of things: the pins coming out of that 2.5mm calculator I/O port and into that orange jack I had "donated" from the other PCB.

The way I had wired things was the Tip of the link cable going into GPIO6 and the Ring going into GPIO7 (worth noting: cable used was TRRS but it changes nothing since the second Ring just shorts to ground anyways).

Got those wired up as constants in my firmware and got to work... basically throwing shit at the wall to map the surface area. Started with the hardware basics:

```py
TIP  = 6 # 0-line
RING = 7 # 1-line

# Start as inputs with pullups (released, pullup wins).
# Pico has no external pullups on these pins, so without Pin.PULL_UP
# the line floats while we are not driving and only the calc-side pullup
# pulls it high; that rise is slow enough to glitch the calc's bit reader
tip  = Pin(TIP,  Pin.IN, Pin.PULL_UP)
ring = Pin(RING, Pin.IN, Pin.PULL_UP)

def release(p):
    p.init(mode=Pin.IN, pull=Pin.PULL_UP)

def pull_low(p):
    p.init(mode=Pin.OUT, value=0)

def idle():
    release(tip); release(ring)

def read():
    # returns (tip_level, ring_level), both 1 = idle
    return tip.value(), ring.value()
```

Idle high, pulled low to talk, ack on the other line: nothing surprising if you've already squinted at the link guide. The thing I didn't appreciate until I was actually sitting there poking at it was how unforgiving the timing is when your "send" function is interpreted Python on a chip that's also doing GC behind your back. The first version of `send_bit` worked beautifully into a logic analyzer and got completely ignored by the calculator. The fix was:

stop being clever, hold the line until the receiver acks, release, wait for the ack to clear, move on.

Once I stopped trying to pace it and just let the two sides clock each other, MicroPython was more than fast enough for my needs.

So with bits going across, bytes are just eight of those LSB-first and a packet is `[machine][cmd][len_lo][len_hi]` followed by an optional body and a 16-bit sum.
I won't labor the framing, the [link guide](https://www.ticalc.org/archives/files/fileinfo/247/24750.html) has it.

The one thing worth flagging for anyone else doing this from scratch is that a handful of the silent-link commands (CTS, ACK, ERR, RDY, EOT, VER) are header-only on the wire even when their length field is nonzero. If you trust the length field you sit there forever waiting for body bytes that are never coming. Ask me how I know... lmao.

The first real milestone was getting the calc to respond to anything at all. I picked `RDY` (0x68) as the hello-world because it's the cheapest possible round trip:
PC sends `[0x23 0x68 0x00 0x00]`, a listening calc replies with an `ACK`, and that's it.

No body, no checksum, no variable headers, just "are you there" / "yes".

The first time I saw the ACK actually come back was cathartic!

From there it was a fairly mechanical climb up the protocol.
`VER` to pull OS and hardware version bytes back (sanity check that I was framing correctly and not just getting lucky on a one-byte command).
Then the variable-transfer dance: the PC sends a `VAR` header describing what's about to land, the calc replies `CTS` if it has room, the PC sends a `DATA` packet with the actual body, calc `ACK`s, PC sends `EOT`.

Four packets to move one variable, and every one of them has to be byte-perfect or the calc just sits there and you have no idea which side dropped the ball.

The thing that unstuck me on the variable transfer was realizing the machine ID isn't the *partner*, it's the *sender*. Both sides stamp their own ID on every packet they put on the wire. So when the calc initiates and sends 0x73, the PC's reply isn't also 0x73, it's 0x23. There's a whole little table of these pairs:

```py
def pc_id_for(calc_id):
    pairs = {0x82: 0x02, 0x83: 0x03, 0x73: 0x23,
             0x86: 0x06, 0x88: 0x08, 0x98: 0x88}
    return pairs.get(calc_id, 0x23)
```

Once that clicked, the rest of it fell into place pretty fast.

The proof-of-life I held myself to was simple. Data across the line, bidirectionally. {4, 2, 0} was, for certain reasons, the packet chosen here... but there were constraints. No tilem, no TI Connect, no CALCnet. MicroPython on the Pico talking the raw protocol to the OS's idle silent-link receiver.

![TODO: photo of the calc home screen with FLAPBIRD freshly received and visible in the program list](../xander_lab_photos/420.png)

That worked. And then, because I didn't trust it, I locked a 2.4KB program, pushed it across, dumped it back, and diffed the bytes. Identical.

At that point I called the wire layer done and started actually thinking about the fun part: what do you put on top of a working bit-banged DBUS stack when you've got a Pico W with wifi sitting one trace away from the link port?

## TIME TO GIVE IT A BRAIN YO

